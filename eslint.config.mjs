// Root ESLint entry. The shared flat config lives in @branchout/config so every app
// and package lints the same way; this file just re-exports it.
export { default } from '@branchout/config/eslint';
