import { RoomsHome } from './RoomsHome';

// The rooms home page. The client component handles the session-dependent host/join actions.
export default function RoomsPage() {
  return <RoomsHome />;
}
