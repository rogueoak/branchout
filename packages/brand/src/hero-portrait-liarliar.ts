import content from '../../../assets/hero-liarliar-portrait.svg';

/** Raw SVG string for the 600x800 portrait Liar Liar hero (the masquerade mask, stacked upright). */
export const heroPortraitLiarLiarSvg: string = content;
