import content from '../../../assets/hero-reversi-portrait.svg';

/** Raw SVG string for the 600x800 portrait Reversi hero (the disc-flip board, stacked upright). */
export const heroPortraitReversiSvg: string = content;
