import content from '../../../assets/hero-loneleaf-portrait.svg';

/** Raw SVG string for the 600x800 portrait Lone Leaf hero (the single oak leaf, stacked upright). */
export const heroPortraitLoneLeafSvg: string = content;
