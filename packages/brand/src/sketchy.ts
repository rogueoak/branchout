import content from '../../../assets/game-sketchy.svg';

/** Raw SVG string for the 512x512 Sketchy game mark (a doodled leaf drawn with a twig on the bark). */
export const sketchySvg: string = content;
