import content from '../../../assets/game-teeter-tower.svg';

/** Raw SVG string for the 512x512 Teeter Tower game mark (a googly-eyed stack teetering upward). */
export const teeterTowerSvg: string = content;
