import content from '../../../assets/game-liarliar.svg';

/** Raw SVG string for the 512x512 Liar Liar game mark (a masquerade mask on a branch-stem stick). */
export const liarLiarSvg: string = content;
