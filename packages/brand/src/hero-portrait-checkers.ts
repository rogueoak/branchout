import content from '../../../assets/hero-checkers-portrait.svg';

/** Raw SVG string for the 600x800 portrait Checkers hero (the board + men and a crowned King, upright). */
export const heroPortraitCheckersSvg: string = content;
