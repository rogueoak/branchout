import content from '../../../assets/game-samebranch.svg';

/** Raw SVG string for the 512x512 Same Branch game mark (a dial branch with a sap-line pointer,
 * a hidden bud band, and the gold-root pivot). */
export const sameBranchSvg: string = content;
