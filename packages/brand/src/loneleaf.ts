import content from '../../../assets/game-loneleaf.svg';

/** Raw SVG string for the 512x512 Lone Leaf game mark (a single oak leaf on a stem to the gold root). */
export const loneLeafSvg: string = content;
