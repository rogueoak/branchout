import content from '../../../assets/game-brambles.svg';

/** Raw SVG string for the 512x512 Brambles game mark (an oak bent into a thorny, blooming vine). */
export const bramblesSvg: string = content;
