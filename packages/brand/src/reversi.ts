import content from '../../../assets/game-reversi.svg';

/** Raw SVG string for the 512x512 Reversi game mark (an oak skeleton bent to a two-tone disc board). */
export const reversiSvg: string = content;
