import content from '../../../assets/game-zinger.svg';

/** Raw SVG string for the 512x512 Zinger game mark (a speech bubble punched through by a zap bolt on
 * an oak stem - the setup and the zinger that lands - anchored by the single gold root). */
export const zingerSvg: string = content;
