import content from '../../../assets/game-chess.svg';

/** Raw SVG string for the 512x512 Chess game mark (an oak-skeleton crowned king carrying the gold root). */
export const chessSvg: string = content;
