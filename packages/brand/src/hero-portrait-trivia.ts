import content from '../../../assets/hero-trivia-portrait.svg';

/** Raw SVG string for the 600x800 portrait Trivia hero (the question-mark branch, stacked upright). */
export const heroPortraitTriviaSvg: string = content;
