import content from '../../../assets/game-odd-bird.svg';

/** Raw SVG string for the 512x512 Odd Bird game mark (a flock perched on an oak branch, with one
 * violet odd bird apart at the tip; the single gold root anchors the branch). */
export const oddBirdSvg: string = content;
